// Re-export clients for convenient imports
// Usage: import { createClient } from '@/lib/supabase/client' (browser)
// Usage: import { createClient } from '@/lib/supabase/server' (server)

// Note: Don't re-export everything from one file to prevent
// accidentally importing server code in client components
